window.addEventListener('DOMContentLoaded', function() {
    var logoBot = document.querySelector('.img-logo-bot-accueil');
    var titleBot = document.querySelector('.title-bot');
    
    // Ajoutez ici le style CSS pour l'animation (par exemple, "fade-in" ou "slide-up")
    logoBot.classList.add('fade-in');
    titleBot.classList.add('slide-up');
});